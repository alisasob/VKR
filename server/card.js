class Card {
    constructor(rank, img, backColor, textColor, number) {
        this.rank = rank;
        this.img = img;
        this.backColor = backColor;
        this.textColor = textColor;
        this.number = number;
    }
};
module.exports.Card = Card;